////////Api request and response////////
routes = {
    user, (done)
    token,  (done)
    cart, (done)
    order, (done),
    pay, (done)
}
(done) userInfo = {
    userId, (done)
    firstName type string, (done)
    lastName type string, (done)
    email type string, (done)
    street_address type string, (done)
    order type object {
        orderId,
        orderCart {
            cartId,
            items [
                {
                    foodType,
                    foodName,
                }
            ],
        }
    }
}

(done) userToken = {
    token type string, (done)
    userId type string, (done)
    email type string, (done)
    expire type DateTime (done)
}

(done) menuItems = {
    foods : {
        food1 : food1Name,
        food2 : food2Name
    },
    drinks : {
        drink1 : drink1Name,
        drink2 : drink2Name
    }
}

(done) cart = {
    cartId,
    items type Array [
        {
            foodType,
            foodName
        },
    ],
    ordered type bool
}

(done) orderInfo = {
    orderId type string,
    cartId type string,
    cartItems type Array [
            {
                foodType,
                foodName,
            }
        ],
    orderedBy type string,
    paid type bool
}

/////////Api////////
(done) users ->
    create user (done)
    update user (done)
    delete user (done)
    get user (done)

(done) token ->
    create userToken (done)
    get userToken (done) 
    update userToken (done) 
    delete userToken (done) 

(done) if user is authenticated ->
    get menuItems

    create cart (done)
    get cart (done)
    update cart (done)
    delete cart (done)

    create order (done)
    get order (done)

    (done) create payment
    (done) get payment
    

(done) create_customer ->

(done) create_payment_method ->

(done) create_payment_intent ->

